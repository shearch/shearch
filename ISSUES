When choosing command, prefered way would be to type that command into shell without executing it.

Whole screen terminal. Desired would be only lowest three lines (zsh like).

Always rebuilds database when called. Desired would be to not kill that process. Maybe with fg/bg/jobs, or separate to client/server.

Python curses. Can this be done with bash/zsh scripts?

Search through description not just tags.

Implement other databases.

Implement website search, recognize and display commands.
